// Este arquivo foi removido pois as rotas antigas de chat (Session e Message)
// não estão mais em uso. O sistema agora usa ConversationSession e ConversationMessage
// através das rotas em /api/chatbot

import { Router } from "express";

const router = Router();

// Rotas antigas removidas - usar /api/chatbot ao invés

export default router;
