version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: artestofados-frontend
    ports:
      - "${FRONTEND_PORT:-4040}:80"
    restart: unless-stopped
    # O backend deve estar na mesma rede Docker com o nome 'backend'
    # Para usar com o docker-compose principal, execute:
    # docker network create estofados-network (se não existir)
    # ou use: docker-compose --project-name estofados up

